# MPShell Runners

This folder contains the MPShell experiment runner scripts.

- `run/` contains single-link and MPTCP execution scripts plus the `test_up` and `test_down` MPShell input files.

The runner scripts consume formatted MPShell timestamp traces from each campaign's local `emulation/formated_traces/<downlink|uplink>/` folder.
